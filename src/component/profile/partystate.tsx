import React from "react";
import styled from "@emotion/styled";

const Container = styled.div`
  display: flex;
  flex-direction: column;
  height: 1200px;
  background-color: #deffbf;
`;

const PartyState = () => {
  return <Container>Partystate</Container>;
};

export default PartyState;
